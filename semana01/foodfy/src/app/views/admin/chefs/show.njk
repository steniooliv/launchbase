{% extends "../layout.njk" %}

{% block content %}

<main>
  
  <div class="recipes">

    <div class="header">
      <h1>Chef: {{chefs.name}}</h1>
      <button type="submit" onclick="window.location.href='/admin/chefs/{{chefs.id}}/edit'">Editar Chef</button>
    </div>

    <div class="chefs-detail">
      <img src="https://github.com/steniooliv.png" alt="{{chefs.name}}">
      <h1>Stenio Oliveira{{chefs.name}}</h1>
      <h3>4 Receitas{{chefs.recipes}}</h2>

      <div class="chefs-recipes">
            
        <h2>Receitas</h2>

        <div class="cards">

          <div class="card">
            <div>
              <img src="/asinhas.png" alt="{[card.title}}" srcset="">
            </div>
            <div>
              <h1>Título da receira</h1>
              <h2>por Stenio Oliveira</h2>
              <a href="/admin/chefs/{{card.id}}" class="link">Visualizar</a>
            </div>
          </div>

          <div class="card">
            <div>
              <img src="/asinhas.png" alt="{[card.title}}" srcset="">
            </div>
            <div>
              <h1>Título da receira</h1>
              <h2>por Stenio Oliveira</h2>
              <a href="/admin/chefs/{{card.id}}" class="link">Visualizar</a>
            </div>
          </div>

          <div class="card">
            <div>
              <img src="/asinhas.png" alt="{[card.title}}" srcset="">
            </div>
            <div>
              <h1>Título da receira</h1>
              <h2>por Stenio Oliveira</h2>
              <a href="/admin/chefs/{{card.id}}" class="link">Visualizar</a>
            </div>
          </div>

          <div class="card">
            <div>
              <img src="/asinhas.png" alt="{[card.title}}" srcset="">
            </div>
            <div>
              <h1>Título da receira</h1>
              <h2>por Stenio Oliveira</h2>
              <a href="/admin/chefs/{{card.id}}" class="link">Visualizar</a>
            </div>
          </div>

        </div>

      </div>
    </div>
  </div>

</main>


{% endblock %}