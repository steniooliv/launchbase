{% extends "../layout.njk" %}

{% block content %}

<main>
  
  <div class="recipes">

    <div class="header">
      <h1>Chef: {{chef.name}}</h1>
      <button type="submit" onclick="window.location.href='/admin/chefs/{{chefs.id}}/edit'">Editar Chef</button>
    </div>

    <div class="chefs-detail">
      <img src="{{chef.avatar_url}}" alt="{{chef.name}}">
      <h1>{{chef.name}}</h1>
      <h3>4 Receitas</h2>

      <div class="chefs-recipes">
            
        <h2>Receitas</h2>

        <div class="cards">

          {% for recipe in recipes %}

            <div class="card">
              <div>
                <img src="{{recipe.image}}" alt="{[recipe.name}}" srcset="">
              </div>
              <div>
                <h1>{{recipe.name}}</h1>
                <h2>por {{chef.name}}</h2>
                <a href="/admin/recipes/{{recipes.id}}" class="link">Visualizar</a>
              </div>
            </div>

          {% endfor %}

        </div>

      </div>
    </div>
  </div>

</main>


{% endblock %}